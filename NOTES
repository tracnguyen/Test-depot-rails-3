Install:
==
  gem install bundler
  bundle install
  install formtastic as plugin: 
    $rails plugin install http://github.com/justinfrench/formtastic.git -r rails3
    $rails g formtastic:install
    
  
