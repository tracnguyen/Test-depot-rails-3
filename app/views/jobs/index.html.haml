%h1 Listing jobs

%table
  %tr
    %th Title
    %th Status
    %th Number of applications
    %th
    %th
    %th

  - @jobs.each do |job|
    %tr
      %td= job.title
      %td= job.status
      %td= job.applicants_count
      %td= link_to 'Public View', pub_job_path(job)
      %td= link_to 'Edit', edit_job_path(job)
      %td= link_to 'Delete', job, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New job', new_job_path
