%h1 Listing accounts

%table
  %tr
    %th Name
    %th Subdomain
    %th
    %th

  - @accounts.each do |account|
    %tr
      %td= account.name
      %td= link_to account.subdomain, root_url(:subdomain => account.subdomain)
      %td= link_to 'Edit', edit_main_account_path(account)
      %td= link_to 'Destroy', main_account_path(account), :confirm => 'Are you sure?', :method => :delete

%br

