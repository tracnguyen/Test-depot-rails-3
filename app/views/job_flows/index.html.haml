%h1 Listing job_flows

%table
  %tr
    %th Account
    %th Job status
    %th
    %th
    %th

  - @job_flows.each do |job_flow|
    %tr
      %td= job_flow.account_id
      %td= job_flow.job_status_id
      %td= link_to 'Show', job_flow
      %td= link_to 'Edit', edit_job_flow_path(job_flow)
      %td= link_to 'Destroy', job_flow, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New job_flow', new_job_flow_path
