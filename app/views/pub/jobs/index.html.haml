- if @jobs.empty?

  %p Thank you for your interest in our company.
  %p Unfortunately there are currently no open positions.
  %p Please check back later!

- else
  %h1 Current openings
  
  %table
    %tr
      %th Title
      %th Description
      %th Status
      %th
  
    - @jobs.each do |job|
      %tr
        %td= job.title
        %td= job.description
        %td= job.status
        %td= link_to 'Show', pub_job_path(job)
